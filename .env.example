# ============================================================
# REQUIRED FOR TELEGRAM
# ============================================================
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ============================================================
# COOLIFY / STORAGE
# ============================================================
PORT=8080
DATA_DIR=/var/data
LOG_LEVEL=INFO

# ============================================================
# PAPER
# ============================================================
PAPER_START_BALANCE=500
MIN_FREE_CASH=5

# ============================================================
# LIVE SAFETY
# Start with 0. After WALLET shows SDK READY + correct wallet/balance,
# change to 1 and redeploy.
# ============================================================
LIVE_MASTER_ENABLE=0

# 0 = do not allow B and C of the SAME token to be LIVE together.
# 1 = allow both; this can send two independent real orders on one market.
ALLOW_DOUBLE_LIVE=0

# Maximum/minimum shares accepted from Telegram SIZE commands.
LIVE_MAX_SHARES_PER_ORDER=1000
LIVE_MIN_SHARES=0.01

# ============================================================
# POLYMARKET WALLET
# Never commit a real private key to GitHub.
# Put these only in Coolify Environment Variables / Secrets.
# ============================================================
POLYMARKET_PRIVATE_KEY=
POLYMARKET_WALLET_ADDRESS=

# Optional. Leave blank unless your wallet setup specifically uses them.
POLYMARKET_RELAYER_API_KEY=
POLYMARKET_RELAYER_API_KEY_ADDRESS=

# ============================================================
# TAKE PROFIT
# Whole-position NET target after entry fee + estimated exit fee.
# 0.30 = close at >= +$0.30 NET.
# OFF or 0 = disable.
# ============================================================
TAKE_PROFIT_USDC=0.30

# ============================================================
# SIGNAL ENGINE — defaults match the tested B/C PAPER bot
# ============================================================
DECISION_INTERVAL=3.0
TRADE_WINDOW_SECONDS=180
ENTRY_ORDER_SIZE=5
DCA_ORDER_SIZE=5
LOOKBACK_TICKS=2
ENTRY_MOVE=0.03

V2_ELIGIBLE_PRICE_MIN=0.55
V2_ELIGIBLE_PRICE_MAX=0.75
V2_ELIGIBLE_MOM_MIN=0.03
V2_ELIGIBLE_MOM_MAX=0.30

SAFE_ENTRY_PRICE_MIN=0.67
SAFE_ENTRY_PRICE_MAX=0.75
SAFE_ENTRY_MOM_MIN=0.05
SAFE_ENTRY_MOM_MAX=0.10

# B DCA
DCA_ARM_PRICE=0.50
DCA_MAX_BUY_PRICE=0.60
DCA_REBOUND_MOM=0.05
DCA_DEADLINE_SEC=120

# C entry + safer DCA
C_SAFE_ENTRY_PRICE_MIN=0.67
C_SAFE_ENTRY_PRICE_MAX=0.70
C_DCA_MIN_BUY_PRICE=0.30
C_DCA_MAX_BUY_PRICE=0.60
C_DCA_REBOUND_MOM_MIN=0.05
C_DCA_REBOUND_MOM_MAX=0.15

MIN_PRICE=0.08
MAX_PRICE=0.95
CRYPTO_FEE_RATE=0.07
DISCOVERY_INTERVAL=10
MAX_BOOK_AGE_MS=1000

MEMORY_CLEANUP_INTERVAL=60
MEMORY_KEEP_RESOLVED_SEC=900
WS_MAX_CONNECTION_AGE_SEC=900
MEMORY_LOG_INTERVAL=300
